name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: curl -sf ${{ secrets.SUPABASE_URL }}/rest/v1/ --max-time 10
